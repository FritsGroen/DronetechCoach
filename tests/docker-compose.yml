version: "3.9"
services:
  dronetech-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: dronetech-backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - ./knowledge:/app/knowledge:ro
